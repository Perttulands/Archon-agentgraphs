# Standalone operations

Use the [current Archon contract](CONTRACT.md#operator-procedure) for build,
authoring, run inputs, gates, cancellation and restart recovery. Host deployment
and forwarding configuration live in the host runbook outside this repository.
